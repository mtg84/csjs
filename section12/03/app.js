// Adding methods to object

const math = {
    numbers: [1,2,3,4,5],
    add: function(x, y) {
		return x + y;
    },
    multiply: function(x,y) {
        return x * y;
    }

};

// when we add a function as a property in  an object we call it a method